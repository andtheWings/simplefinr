% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transactions.R
\name{sfin_transactions}
\alias{sfin_transactions}
\title{Retrieve transactions from a SimpleFIN server}
\usage{
sfin_transactions(
  access_url = NULL,
  key = NULL,
  start_date = NULL,
  end_date = NULL,
  pending = FALSE,
  account = NULL,
  join_accounts = FALSE
)
}
\arguments{
\item{access_url}{Character. The Access URL returned by \code{\link[=sfin_claim_token]{sfin_claim_token()}}.
Must be an HTTPS URL with embedded Basic Auth credentials in the form
\verb{https://username:password@host/path}. Provide either this or \code{key},
not both.}

\item{key}{Character. A keyring key name set via \code{\link[=sfin_set_access_url]{sfin_set_access_url()}}.
When supplied the Access URL is retrieved from the OS credential store
and \code{access_url} must be \code{NULL}. Requires the \pkg{keyring} package.}

\item{start_date}{Optional. Restrict transactions to those posted on or
after this date. Accepts a \code{Date}, \code{POSIXct}, or a numeric UNIX timestamp.}

\item{end_date}{Optional. Restrict transactions to those posted before
(\strong{not including}) this date. Accepts a \code{Date}, \code{POSIXct}, or a numeric
UNIX timestamp.}

\item{pending}{Logical. If \code{TRUE}, include pending transactions (if
supported by the server). Default \code{FALSE}.}

\item{account}{Optional character vector of account IDs. If provided, only
data for those accounts is returned. May list multiple IDs.}

\item{join_accounts}{Logical. If \code{TRUE}, left-join the \code{accounts} tibble
onto the transactions by \code{account_id}, adding columns \code{account_name},
\code{conn_id}, \code{conn_name}, and \code{currency} to every transaction row.
Default \code{FALSE}.}
}
\value{
A tibble with one row per transaction. Columns when
\code{join_accounts = FALSE}:\tabular{lll}{
   Column \tab Type \tab Description \cr
   \code{account_id} \tab chr \tab Account the transaction belongs to \cr
   \code{transaction_id} \tab chr \tab Unique transaction ID within the account \cr
   \code{posted} \tab POSIXct (UTC) \tab Post date; \code{NA} for pending transactions \cr
   \code{transacted_at} \tab POSIXct (UTC) \tab Transaction date, if provided \cr
   \code{amount} \tab dbl \tab Positive = deposit, negative = withdrawal \cr
   \code{description} \tab chr \tab Human-readable description \cr
   \code{pending} \tab lgl \tab \code{TRUE} if not yet posted \cr
}


When \code{join_accounts = TRUE}, four additional columns are prepended after
\code{account_id}: \code{account_name}, \code{conn_id}, \code{conn_name}, \code{currency}.
}
\description{
A convenience wrapper around \code{\link[=sfin_accounts]{sfin_accounts()}} that returns only the
transactions tibble. Optionally left-joins account-level metadata
(name, currency, connection) onto each transaction row.
}
\details{
Any server-level errors are surfaced as warnings (same as \code{\link[=sfin_accounts]{sfin_accounts()}}).
Inspect them by calling \code{\link[=sfin_accounts]{sfin_accounts()}} directly and checking
\code{result$errors}.
}
\examples{
\dontrun{
# Using a literal Access URL
access_url <- Sys.getenv("SIMPLEFIN_ACCESS_URL")
sfin_transactions(access_url, start_date = Sys.Date() - 30)

# Using a keyring key (requires the keyring package)
sfin_transactions(key = "personal", start_date = Sys.Date() - 30)

# With account metadata joined in
sfin_transactions(key = "personal", start_date = Sys.Date() - 30,
                  join_accounts = TRUE)
}

}
