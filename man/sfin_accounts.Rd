% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accounts.R
\name{sfin_accounts}
\alias{sfin_accounts}
\title{Retrieve accounts and transactions from a SimpleFIN server}
\usage{
sfin_accounts(
  access_url = NULL,
  key = NULL,
  start_date = NULL,
  end_date = NULL,
  pending = FALSE,
  account = NULL,
  balances_only = FALSE
)
}
\arguments{
\item{access_url}{Character. The Access URL returned by \code{\link[=sfin_claim_token]{sfin_claim_token()}}.
Must be an HTTPS URL with embedded Basic Auth credentials in the form
\verb{https://username:password@host/path}. Provide either this or \code{key},
not both.}

\item{key}{Character. A keyring key name set via \code{\link[=sfin_set_access_url]{sfin_set_access_url()}}.
When supplied the Access URL is retrieved from the OS credential store
and \code{access_url} must be \code{NULL}. Requires the \pkg{keyring} package.}

\item{start_date}{Optional. Restrict transactions to those posted on or
after this date. Accepts a \code{Date}, \code{POSIXct}, or a numeric UNIX timestamp.}

\item{end_date}{Optional. Restrict transactions to those posted before
(\strong{not including}) this date. Accepts a \code{Date}, \code{POSIXct}, or a numeric
UNIX timestamp.}

\item{pending}{Logical. If \code{TRUE}, include pending transactions (if
supported by the server). Default \code{FALSE}.}

\item{account}{Optional character vector of account IDs. If provided, only
data for those accounts is returned. May list multiple IDs.}

\item{balances_only}{Logical. If \code{TRUE}, skip transaction data and return
only account balances. Default \code{FALSE}.}
}
\value{
A named list with four tibbles:
\itemize{
\item \strong{\code{accounts}} — one row per account with columns:
\code{account_id}, \code{account_name}, \code{conn_id}, \code{conn_name}, \code{currency},
\code{balance} (numeric), \code{available_balance} (numeric),
\code{balance_date} (POSIXct UTC).
\item \strong{\code{transactions}} — one row per transaction with columns:
\code{account_id}, \code{transaction_id}, \code{posted} (POSIXct UTC),
\code{transacted_at} (POSIXct UTC), \code{amount} (numeric), \code{description},
\code{pending} (logical). \code{posted} is \code{NA} for pending transactions.
\item \strong{\code{connections}} — one row per connection with columns:
\code{conn_id}, \code{name}, \code{org_id}, \code{org_url}, \code{sfin_url}.
\item \strong{\code{errors}} — one row per structured error with columns:
\code{code}, \code{msg}, \code{conn_id}, \code{account_id}.
}
}
\description{
Issues a \code{GET /accounts} request to the SimpleFIN Access URL and returns
the response as a named list of four flattened tibbles: \code{accounts},
\code{transactions}, \code{connections}, and \code{errors}.
}
\details{
The function raises an error on HTTP 403 (access revoked or bad credentials)
and HTTP 402 (payment required). A non-empty \code{errors} tibble in the return
value indicates partial failures at the connection or account level; inspect
these alongside the other tibbles rather than treating the call as failed.

Timestamps from the API are UNIX epoch integers and are converted to
\code{POSIXct} in UTC. Balance and amount fields are returned as numeric
(the API represents them as strings).
}
\examples{
\dontrun{
# Using a literal Access URL
access_url <- Sys.getenv("SIMPLEFIN_ACCESS_URL")
result <- sfin_accounts(access_url, start_date = Sys.Date() - 30)

# Using a keyring key (requires the keyring package)
sfin_set_access_url(access_url, key = "personal")
result <- sfin_accounts(key = "personal", start_date = Sys.Date() - 30)

result$accounts
result$transactions
result$errors
}

}
